
services:
  gtapp:
    # image: gt
    build:
      context: .
    container_name: giorgiotedesco
    environment:
      - NODE_ENV=dev
      - UID=1000
      - GID=1000
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
    user: "${UID}:${GID}"
    volumes:
      - ./content:/app/content
      - ./src:/app/src
      - ./public:/app/public
      - ./package.json:/app/package.json
      - ./package-lock.json:/app/package-lock.json
      - /home/giorgionetg/WebSiteLogSeq:/app/logseq
      - /app/node_modules
    ports:
      - "3000:3000"
    dns:
      - 1.1.1.1
      - 8.8.8.8
    command: yarn dev